import { Controller } from '@nestjs/common';

/**
 * Adapter de entrada HTTP del módulo `admin-query`.
 *
 * Esqueleto vacío: no declara rutas funcionales (fuera de alcance de
 * MSF-API-001). Cuando se implemente, será solo lectura de órdenes; nunca
 * contendrá reglas de negocio ni Prisma.
 */
@Controller()
export class AdminQueryController {}
